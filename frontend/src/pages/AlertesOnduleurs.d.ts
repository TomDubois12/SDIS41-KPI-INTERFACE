/**
 * Composant React représentant la page d'affichage des alertes/emails spécifiques aux Onduleurs.
 * Sert principalement de conteneur pour afficher l'en-tête standard (`Header`)
 * et le composant `EmailOnduleur` qui contient la logique et l'affichage de ces alertes.
 *
 * @returns Le composant JSX de la page des alertes Onduleurs.
 */
export default function AlertesOnduleurs(): import("react/jsx-runtime").JSX.Element;
